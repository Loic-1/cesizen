api_platform:
    title: Bookshop API
    version: 1.0.0
    formats:
        json:
            mime_types:
                - application/json
    patch_formats:
        json:
            mime_types:
                - application/merge-patch+json
    defaults:
        stateless: true
        cache_headers:
            vary: ['Content-Type', 'Authorization', 'Origin']
    swagger:
        api_keys:
            bearerAuth:
                name: Authorization
                type: header
