# yaml-language-server: $schema=../vendor/symfony/dependency-injection/Loader/schema/services.schema.json

# This file is the entry point to configure your own services.
# Files in the packages/ subdirectory configure your dependencies.
# See also https://symfony.com/doc/current/service_container/import.html

# Put parameters here that don't need to change on each machine where the app is deployed
# https://symfony.com/doc/current/best_practices.html#use-parameters-for-application-configuration
parameters:
    app.jwt_secret: '%env(JWT_SECRET)%'
    app.jwt_ttl: '%env(int:JWT_TTL)%'
    app.refresh_token_ttl: '%env(int:REFRESH_TOKEN_TTL)%'
    app.refresh_token_cookie_name: '%env(REFRESH_TOKEN_COOKIE_NAME)%'
    app.refresh_token_cookie_path: '%env(REFRESH_TOKEN_COOKIE_PATH)%'
    app.refresh_token_cookie_same_site: '%env(REFRESH_TOKEN_COOKIE_SAMESITE)%'
    app.refresh_token_cookie_secure: '%env(bool:REFRESH_TOKEN_COOKIE_SECURE)%'
    app.email_verification_ttl: '%env(int:EMAIL_VERIFICATION_TTL)%'
    app.mailer_from_email: '%env(MAILER_FROM_EMAIL)%'

services:
    # default configuration for services in *this* file
    _defaults:
        autowire: true      # Automatically injects dependencies in your services.
        autoconfigure: true # Automatically registers your services as commands, event subscribers, etc.

    # makes classes in src/ available to be used as services
    # this creates a service per class whose id is the fully-qualified class name
    App\:
        resource: '../src/'

    App\Security\JwtTokenManager:
        arguments:
            $jwtSecret: '%app.jwt_secret%'
            $jwtTtl: '%app.jwt_ttl%'

    App\Service\RefreshTokenManager:
        arguments:
            $refreshTokenTtl: '%app.refresh_token_ttl%'

    App\Service\EmailVerificationManager:
        arguments:
            $emailVerificationTtl: '%app.email_verification_ttl%'

    App\Service\RefreshTokenCookieManager:
        arguments:
            $cookieName: '%app.refresh_token_cookie_name%'
            $cookiePath: '%app.refresh_token_cookie_path%'
            $sameSite: '%app.refresh_token_cookie_same_site%'
            $secure: '%app.refresh_token_cookie_secure%'

    App\Service\VerificationEmailSender:
        arguments:
            $fromEmail: '%app.mailer_from_email%'
            $appEnv: '%env(APP_ENV)%'

    # add more service definitions when explicit configuration is needed
    # please note that last definitions always *replace* previous ones
